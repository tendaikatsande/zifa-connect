APP_NAME="ZIFA Connect"
APP_ENV=local
APP_KEY=
APP_DEBUG=true
APP_TIMEZONE=Africa/Harare
APP_URL=http://localhost

APP_LOCALE=en
APP_FALLBACK_LOCALE=en
APP_FAKER_LOCALE=en_US

APP_MAINTENANCE_DRIVER=file

PHP_CLI_SERVER_WORKERS=4

BCRYPT_ROUNDS=12

LOG_CHANNEL=stack
LOG_STACK=single
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=debug

DB_CONNECTION=pgsql
DB_HOST=127.0.0.1
DB_PORT=5432
DB_DATABASE=zifa_connect
DB_USERNAME=zifa
DB_PASSWORD=

SESSION_DRIVER=database
SESSION_LIFETIME=120
SESSION_ENCRYPT=false
SESSION_PATH=/
SESSION_DOMAIN=null

BROADCAST_CONNECTION=log
FILESYSTEM_DISK=local
QUEUE_CONNECTION=database

CACHE_STORE=database
CACHE_PREFIX=

MEMCACHED_HOST=127.0.0.1

REDIS_CLIENT=phpredis
REDIS_HOST=127.0.0.1
REDIS_PASSWORD=null
REDIS_PORT=6379

MAIL_MAILER=log
MAIL_HOST=127.0.0.1
MAIL_PORT=2525
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_ENCRYPTION=null
MAIL_FROM_ADDRESS="noreply@zifa.org.zw"
MAIL_FROM_NAME="${APP_NAME}"

AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=
AWS_USE_PATH_STYLE_ENDPOINT=false

# MinIO Object Storage (S3-compatible)
MINIO_ACCESS_KEY=minio
MINIO_SECRET_KEY=minio123
MINIO_REGION=us-east-1
MINIO_BUCKET=zifa-connect
MINIO_URL=http://localhost:9000
MINIO_ENDPOINT=http://localhost:9000

# Use 's3' or 'minio' for production object storage
FILESYSTEM_DOCUMENTS=local

VITE_APP_NAME="${APP_NAME}"

# PesePay Configuration
PESEPAY_INTEGRATION_KEY=
PESEPAY_ENCRYPTION_KEY=
PESEPAY_BASE_URL=https://api.pesepay.com/api/payments-engine/v1
PESEPAY_RETURN_URL="${APP_URL}/payments/callback"
PESEPAY_RESULT_URL="${APP_URL}/api/payments/webhook"

# FIFA Connect Configuration
FIFA_CONNECT_API_URL=
FIFA_CONNECT_API_KEY=
FIFA_CONNECT_CLIENT_ID=
FIFA_CONNECT_CLIENT_SECRET=
